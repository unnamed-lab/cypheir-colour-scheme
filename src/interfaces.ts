export interface RGB {
  red: number;
  green: number;
  blue: number;
  alpha?: number;
}

export interface HSL {
  hue: number;
  saturation: number;
  lightness: number;
}

// export interface pALPHA {
//   primary: string | RGB;
//   secondary: string | RGB;
//   accent: string | RGB;
// }